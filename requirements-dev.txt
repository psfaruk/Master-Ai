# Test-only dependencies — NOT installed in the Railway runtime image.
# Use this when running the test suite locally:
#   pip install -r requirements.txt -r requirements-dev.txt && pytest

pytest==8.3.4
pytest-asyncio==0.25.2
pyflakes==3.3.2
